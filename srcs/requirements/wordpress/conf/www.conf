[www]
user  = nobody
group = nobody

listen = 9000

pm                   = dynamic
pm.max_children      = 5
pm.start_servers     = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

; Pass all environment variables (including those from env_file) into PHP
clear_env = no
